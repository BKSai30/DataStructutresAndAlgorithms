Neha is working on a postfix expression converter that should build an expression tree and print its prefix form. However, her current program produces incorrect results due to logical errors in stack handling, operand/operator order, and incorrect traversal logic. 



Write a program to debug and fix the code so that it correctly converts a postfix expression into a prefix expression using an expression tree and displays the output in prefix order.

Input format :
The input consists of a single string representing a valid postfix expression (without spaces).

Output format :
The output prints a string representing the corresponding prefix expression.



Refer to the sample output for the formatting specifications.

Code constraints :
1 ≤ Length of postfix expression ≤ 100

The expression is guaranteed to be valid.

Operands consist only of alphabetic characters (a–z, A–Z).

Sample test cases :
Input 1 :
ab+cd-*
Output 1 :
*+ab-cd
Input 2 :
ABC/-AK/L-*
Output 2 :
*-A/BC-/AKL
Note :
The program will be evaluated only after the “Submit Code” is clicked.
Extra spaces and new line characters in the program output will result in the failure of the test case.




Arjun is implementing an infix-to-postfix converter using an expression tree but finds that his current code outputs incorrect postfix expressions due to multiple logical bugs. 



The goal is to debug the program so that it correctly constructs an expression tree from an infix expression and prints the corresponding postfix form by performing a post-order traversal. The objective is to ensure the output correctly represents the postfix equivalent of the given infix expression.

Input format :
The input consists of a single string representing a valid infix expression (may contain operands a–z, A–Z, and operators +, -, *, / with parentheses).

Output format :
The output prints a string representing the equivalent postfix expression of the given infix expression.



Refer to the sample output for the formatting specifications.

Code constraints :
1 ≤ Expression length ≤ 50

Operands are limited to uppercase or lowercase English letters.

The expression will be valid and fully parenthesized where necessary.

Sample test cases :
Input 1 :
a+b+c
Output 1 :
ab+c+
Input 2 :
a*b+c
Output 2 :
ab*c+
Note :
The program will be evaluated only after the “Submit Code” is clicked.
Extra spaces and new line characters in the program output will result in the failure of the test case.
Marks : 10
Negative Marks : 0





Reena is experimenting with binary tree operations. She wants to construct a binary tree in level order from a given set of node values and print only the nodes with odd values during an in-order traversal. However, her initial implementation has logical errors in node creation, insertion, and traversal. 



Your task is to debug the program so that it correctly constructs the tree and prints only odd-valued nodes in the proper in-order sequence.

Input format :
The first line contains an integer n, representing the number of nodes in the binary tree.

The second line contains n space-separated integers, representing the values of the nodes.

Output format :
The output prints a single line containing all nodes with odd values, obtained through in-order traversal of the binary tree.

Values should be printed in the traversal order and separated by spaces.



Refer to the sample output for the formatting specifications.

Code constraints :
The input integers should be unique.

1 ≤ n ≤ 20

1 ≤ Node values ≤ 100

All node values are unique.

The binary tree contains at least one node with an odd value.

Sample test cases :
Input 1 :
3
3 4 2
Output 1 :
3 
Input 2 :
5
7 5 12 2 3
Output 2 :
5 3 7 
Note :
The program will be evaluated only after the “Submit Code” is clicked.
Extra spaces and new line characters in the program output will result in the failure of the test case.
Marks : 10
Negative M






Saniya is experimenting with binary tree operations. She wants to construct a level-order binary tree from a given set of node values and print only the parent nodes during a post-order traversal. Her initial implementation contains logical errors in node creation, insertion, and traversal. 



Your task is to debug the program so that it correctly constructs the tree and prints only parent nodes in proper post-order sequence.

Input format :
The first line contains an integer n, representing the number of nodes in the binary tree.

The second line contains n space-separated integers representing the values of the nodes.

Output format :
The output prints a single line containing the values of parent nodes obtained through post-order traversal.

Values should be printed in traversal order, separated by spaces.



Refer to the sample output for the formatting specifications.

Code constraints :
1 ≤ n ≤ 15

1 ≤ Node values ≤ 20

A parent node has at least one child node (either left, right, or both).

The input integers should be unique.

Sample test cases :
Input 1 :
3
3 4 2
Output 1 :
3 
Input 2 :
5
8 4 12 2 6
Output 2 :
4 8 
Note :
The program will be evaluated only after the “Submit Code” is clicked.
Extra spaces and new line characters in the program output will result in the failure of the test case.
Marks : 10
Negative Marks : 0





Arjun is implementing a program to evaluate prefix arithmetic expressions using a binary expression tree. However, his initial approach produces incorrect results due to logical errors in tree construction and evaluation. 



The goal is to debug and implement a program that correctly constructs a binary expression tree from a prefix expression and evaluates its result. The expression will contain only single-digit operands (0–9) and operators +, -, *, /.

Input format :
The input consists of a single line containing a valid prefix expression as a string.

The expression will consist of single-digit operands and the operators +, -, *, /.

Output format :
The output prints "Result: <evaluated_value>" representing the computed result of the expression.



Refer to the sample output for the formatting specifications.

Code constraints :
1 ≤ Length of prefix expression ≤ 100.

Operands are single-digit numbers (0–9).

Operators include +, -, *, /.

Sample test cases :
Input 1 :
+35
Output 1 :
Result: 8
Input 2 :
*+726
Output 2 :
Result: 54
Note :
The program will be evaluated only after the “Submit Code” is clicked.
Extra spaces and new line characters in the program output will result in the failure of the test case.
Marks : 10
Negative M



Dhanu wants to construct a binary tree in level order from a given set of node values and perform a pre-order traversal. During the traversal, she needs to print all node values and calculate the sum of all nodes in the tree. Her initial implementation has logical errors in node creation, insertion, and traversal. 



Your task is to debug the program so that it correctly constructs the tree, prints the pre-order traversal, and computes the sum.

Input format :
The first line contains an integer n, representing the number of nodes in the binary tree.

The second line contains n space-separated integers representing the values of the nodes.

Output format :
The first line prints "Preorder Traversal: " followed by the pre-order traversal of the constructed binary tree.

The second line prints "Sum: " followed by the sum of all node values in the binary tree.



Refer to the sample output for the formatting specifications.

Code constraints :
1 ≤ n ≤ 15

1 ≤ Node values ≤ 150

Sample test cases :
Input 1 :
3
50 30 70
Output 1 :
Preorder Traversal: 50 30 70 
Sum: 150
Input 2 :
5
8 4 12 2 6
Output 2 :
Preorder Traversal: 8 4 2 6 12 
Sum: 32
Note :
The program will be evaluated only after the “Submit Code” is clicked.
Extra spaces and new line characters in the program output will result in the failure of the test case.
Marks : 10
Negative Marks : 0